#include <stdio.h>

/*
Reading 4 integers in a single scanf statement and print them (using a single
prinJ statement) in separate lines such that the last digit of each integer 
is exactly 10 spaces away from the beginning of the line it is printed in (the 9 spaces before will be occupied by blanks or other digits of the integer).
Remember that different integers can have different number of digits.
*/

int main() {
    
    return 0;
}