#include <stdio.h>

/*
Using scanf() to read two integers and two floating point numbers, each in a
separate statement (so 4 scanfs) and print them separately using printf (4
printfs) with some nice comments.
*/

int main() {
    // NOTE: do not change name and type of the variables
    int num1, num2;
    float num3, num4;
    
    // Read 2 integers

    // Read 2 floating point numbers

    // Print in following order: num1, num2, num3, num4
    return 0;
}